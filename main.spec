# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['D:/Machine Learning Projects/Face Emotion based Music Player/main.py'],
    pathex=[],
    binaries=[],
    datas=[('D:/Machine Learning Projects/Face Emotion based Music Player', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='main',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['D:\\Machine Learning Projects\\Face Emotion based Music Player\\Resources\\Untitled design.ico'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='main',
)
